<%= button_to 'Top Rated Movies', movies_path, method: :get, params: {top: "top_rated"} %>

<%= form_with url:  movies_path, method: :get, local: true  do |form| %>
  <%= form.label :Movie_Name %>
  <%= form.text_field :name %>

  <%= form.submit "Search for movies" %>
<% end %>
